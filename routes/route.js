/*
* Define the endpoints, interacts with the controller
*/
const express = require('express');
const router = express.Router();

// Example
// const controller = require('../controllers/controller');
// router.get('/', controller.get);
// module.exports = router;